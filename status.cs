namespace JobApplicationTracker
{
    // Klassen innehåller en enum som visar status för en jobbansökan
    public enum Status
    {
        Applied,    // Ansökan skickad
        Interview,  // Intervju bokad eller genomförd
        Offer,      // Erbjudande fått
        Rejected    // Fått avslag
    }
}
